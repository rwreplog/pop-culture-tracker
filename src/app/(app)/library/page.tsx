import { Library } from "lucide-react";

import { PlaceholderScreen } from "@/components/layout/placeholder-screen";

export default function LibraryPage() {
  return (
    <PlaceholderScreen
      icon={Library}
      title="Your library"
      description="Everything you're tracking will show up here — with search, filters, and sorting."
    />
  );
}
